// que e loop no javascript
// loops são estruturas de repetição que permitem executar um bloco de código várias vezes
// em javascript existem 3 tipos de loops principais: for, while e do while
// for é usado quando sabemos o número de iterações
// while é usado quando não sabemos o número de iterações

const numero = 11;
if (numero % 2 === 0) {
  console.log("O número " + numero + "é par");
} else {
  console.log("O " + numero + "é ímpar");
}
